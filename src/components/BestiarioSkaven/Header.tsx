import * as React from 'react';
import { View, Text } from 'react-native';

export interface headerSkavenProps {
}

export function headerSkavenInfo (props: headerSkavenProps) {
    return (
      <View>
         <Text>headerSkaven</Text>
      </View>
    );
}
